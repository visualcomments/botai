# Программа: что такое различие версий

## Модуль 1. Файл, индекс и коммит

Цели:
- `explain-diff` — объяснить своими словами, что сравнивает `git diff`.
- `apply-staging` — предсказать, что попадёт в коммит после `git add`.

Практика: `assignments/practice-diff.md` (не оценивается).
Оцениваемое задание в этом примере **отсутствует** намеренно: в учебном
примере нет скрытых ключей, которые можно было бы подсмотреть.

Предпосылки: базовое умение открыть терминал и файл.
